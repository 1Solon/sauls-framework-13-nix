{ ... }:

{
  programs.vscode = {
    enable = true;
    # userSettings = {
    #   # Disable restoring previous session - always open new window with specified folder
    #   "window.restoreWindows" = "none";
      
    #   # Don't restore editors from previous session
    #   "workbench.startup.editor" = "none";
    # };
  };
}
